<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NossoAppe"
        tools:targetApi="31" >
        <activity android:name=".controller.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.LoginActivity"/>
        <activity android:name=".controller.MenuActivity"/>
        <activity android:name=".controller.CadastroMActivity"/>
        <activity android:name=".controller.CadastroGActivity"/>
        <activity android:name=".controller.ListaMActivity"/>
        <activity android:name=".controller.ListaGActivity"/>
        <activity android:name=".controller.MudarSenhaActivity"/>
        <activity android:name=".controller.EditarMoradorActivity" />
        <activity android:name=".controller.EditarGastoActivity" />
        <activity android:name=".controller.ListadeGastos" />

    </application>

</manifest>